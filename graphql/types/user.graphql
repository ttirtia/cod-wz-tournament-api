type User {
  id: ID!
  username: String!
  password: String!
  isAdmin: Boolean!
  createdAt: Date!
  updatedAt: Date
}

type Mutation {
  login(username: String!, password: String!): String!
}

input UserFilter {
  id: ID
  username: String
  isAdmin: Boolean
}

type Query {
  findUsers(filter: UserFilter): [User]
}
